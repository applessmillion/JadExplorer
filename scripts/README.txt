Scripts will input data using the files located in /web/e/.
The only thing you need to change is the URL of the file these scripts are visiting.
The URL can be found near the top of each script as the $visit_URL variable.